import MovieContainer from "../containers/MovieContainer"


const ShowScreen = ({ navigation, route }) => (
  <MovieContainer navigation={navigation} route={route}></MovieContainer>
)

export default ShowScreen